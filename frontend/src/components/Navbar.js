import React from 'react';

const Navbar = (any) = {
    return (
        <nav className="navbar">
            <a className="navbar-brand" href="#">
                Navbar
            </a>
        </nav>
    );
};

export default Navbar;